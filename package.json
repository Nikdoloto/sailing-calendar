{
  "name": "sailing-team-calendar",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first PWA calendar for sailing team training availability.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
